<div class="container">
<%= simple_form_for [@booking, @review] do |f| %>
  <%= f.input :rating %>
  <%= f.input :description %>
  <%= f.button :submit %>
<% end %>
</div>
<%= link_to "Back", dashboard_path, class: 'btn btn-light mx-2' %>
