<div>
<% @flats.each do |flat| %>
<h1><%=flat.name %></h1>
<p><%=flat.description %></p>
<p><%=flat.address%></p>
<p><%=flat.daily_price%></p>
<p><%=flat.longitude%></p>
<p><%=flat.latitude%></p>
<p><%=flat.features%></p>
<br>
<%= link_to 'See this apartment', flat_path(flat), class: 'button'%>
<% end %>
</div>

<div>
<%= link_to 'Add your own flat', new_flat_path, class: 'button' %>
</div>
<div>
<%= link_to 'your profile', dashboard_path, class: 'button' %>
</div>


<div
  id="map"
  style="width: 100%;
  height: 600px;"
  data-markers="<%= @markers.to_json %>"
  data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"
>ciao sono qua</div>


